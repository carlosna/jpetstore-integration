plugins {
    id 'ear'
    id 'java'
}

repositories { mavenCentral() }

dependencies {
    deploy project(path: ':jpetstore-war', configuration: 'archives')
    deploy project(path: ':jpetstore-ejb', configuration: 'archives')
}

ear {
    appDirName 'src/main/app' 
}

baseName = 'jpetstore'
